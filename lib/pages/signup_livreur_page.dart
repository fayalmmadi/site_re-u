import 'package:flutter/material.dart';

class SignupLivreurPage extends StatelessWidget {
  const SignupLivreurPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Inscription Livreur')),
      body: const Center(
        child: Text('TODO: formulaire inscription livreur'),
      ),
    );
  }
}
